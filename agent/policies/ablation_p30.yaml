name: ablation_p30
arm_id: P30
mode: predictive_guarded

target:
  namespace: workload
  deployment: workload-cloud
  pod_prefix: workload-cloud
  tier: cloud

bounds:
  min_replicas: 1
  max_replicas: 6

cooldowns:
  scale_up_seconds:   30
  scale_down_seconds: 180

prediction_enabled_in_decision: true

model_path: ablation_p30_model.json
